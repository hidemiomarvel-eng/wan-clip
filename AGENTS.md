# AGENTS.md

このリポジトリは、犬と一緒に利用できるスポットを共有する Web アプリ「WanClip」の初期実装です。AI エージェントは、ユーザーとの会話やコード作成の際に、以下の方針を優先してください。

- 日本語での対応を基本とする
- 仕様書と既存の実装を先に確認してから変更する
- 学習目的のプロジェクトなので、Next.js・TypeScript・Tailwind CSS の基本的な実装を優先する
- 必要以上に複雑な設計や新しいライブラリを追加しない
- V1 の範囲を守り、将来機能を先取りしない
- 最小変更で、既存の構造と命名規則に沿って実装する

## 参照ドキュメント

- [README.md](README.md)
- [docs/specification.md](docs/specification.md)

## プロジェクト概要

- Next.js + TypeScript + Tailwind CSS + App Router を使用
- 現時点では DB・認証・外部API連携は導入されていない
- V1 の対象は、スポット一覧・詳細・登録・編集・削除・検索・絞り込み
- 将来的なユーザー管理、口コミ、写真、旅行プラン等は未実装前提

## よく使うコマンド

```bash
npm install
npm run dev
npm run build
npm run lint
```

## 作業開始時の必須確認

- すべての作業は開始前に [docs/specification.md](docs/specification.md) を確認する
- 現在の実装を確認し、既存の型・モックデータ・コンポーネント構成との整合を取る
- 代表的な確認対象は [types/spot.ts](types/spot.ts)、[lib/mock-data.ts](lib/mock-data.ts)、[app/spots/page.tsx](app/spots/page.tsx)、[components/spots/SpotCard.tsx](components/spots/SpotCard.tsx) を含む
- 変更前に、どの画面・データモデル・UI 組み合わせに影響するかを把握してから修正する
- 既存の命名規則と構造を守り、仕様書と実装の両方に矛盾しない変更を行う

## 実装時の指針

- 画面構成は App Router の `app/` 配下を中心に扱う
- 仕様の軸は `docs/specification.md` にある V1 定義を優先する
- 未定義の機能や過剰な設計を勝手に追加しない
- 既存の initial scaffold を壊さず、段階的に改善する
- UI はシンプルで読みやすく、犬連れ向けの情報が伝わる設計を目指す

## 現在の実装状態

- 現在はモックデータを使ったホーム画面、スポット一覧、`SpotCard` が中心。仕様書にある未実装画面や操作を依頼なしに先行実装しない
- ページはサーバーコンポーネントを基本とし、検索やフォームなどの操作に必要な場合だけクライアントコンポーネントを使う
- データを変更するときは、`types/spot.ts`、`lib/mock-data.ts`、表示用ラベルの整合性を確認する
- `Spot` の任意項目は未確認状態を許容し、画面で空値を不自然に表示しない

## 追加・変更時の注意

- 型定義やデータ構造は、仕様書の `Spot` / `SpotFilter` を参考にする
- 機能追加の前に、まず画面とデータモデルの整合を確認する
- モックデータを使う場合は、仕様書の範囲に沿って制限する
- 認証や永続化を追加したい場合は、明示的に依頼がある場合のみ対応する
- 依存関係を追加する前に、標準機能または既存の Next.js・TypeScript・Tailwind CSS で解決できないか確認する

## 検証

- 変更後は、可能な範囲で `npm run lint` と `npm run build` を実行する
- 専用の型チェックコマンドはないため、型の変更を含む場合は `npm run build` で確認する

## 期待する作業スタイル

- 変更が必要な箇所を最小限に絞る
- 既存のファイルを尊重して、既存の設計意図を損なわない
- 変更の理由を簡潔に説明し、必要なら補足ドキュメントを更新する
- 会話は基本的に日本語で行い、ユーザーが英語を要求した場合のみ英語に切り替える
